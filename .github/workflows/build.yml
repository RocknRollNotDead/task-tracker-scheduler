name: Build & Push Docker Image

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & push image
        run: |
          docker build -t appleapplenotdead/task-tracker-scheduler:${{ github.sha }} .
          docker push appleapplenotdead/task-tracker-scheduler:${{ github.sha }}
          
          docker tag appleapplenotdead/task-tracker-scheduler:${{ github.sha }} appleapplenotdead/task-tracker-scheduler:latest
          docker push appleapplenotdead/task-tracker-scheduler:latest